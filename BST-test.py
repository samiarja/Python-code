#write a function which take the given array and find and return the
#second biggest number
import time
start = time.time()
    #write a function that takes two strings and returns True if they are reverse
def sum_array(list_array):
    total=0
    list_array=sum(list_array)
    total+=list_array
    return total
print(sum_array([100,100]))

#Write a function that take an array of string and convert it to uppercase
def upper_case(s1,s2):
    return s1.upper() == s2.upper()
print(upper_case("abc","ABc"))

#Compute the time complexity
my_list=[]
for i in range(10):
    my_list.append(i)
elapsed_time_lc=(time.time()-start)
print(my_list)

        
